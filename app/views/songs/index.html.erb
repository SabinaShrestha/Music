<h1><%= @artist.name %></h1>
<ul>
  <% @songs.each do |s| %>
    <li>
      <%= s.title %>
      <%= s.rating %>
      <%= link_to, 'Edit song' edit_artist_song_path[@artist, s] %>
    </li>
  <% end %>
</ul>
